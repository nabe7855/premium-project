# ホテルサイト（Sweet Stay）開発進捗 & 引き継ぎガイド

本ドキュメントは、ホテルポータルサイト「Sweet Stay」の開発進捗、データベース構造、および今後のタスクをまとめたものです。

## 1. プロジェクト概要

全国のラブホテル情報を網羅し、AIを活用した口コミ生成とリッチなコンテンツ提供を行う次世代ホテルポータル。

## 2. 現在の進捗ステータス（2026年3月7日時点）

| 項目                   | ステータス       | 詳細                                                                     |
| :--------------------- | :--------------- | :----------------------------------------------------------------------- |
| **マスターデータ**     | **完了**         | 3,759件の名称・住所・電話番号が `lh_hotels` に登録済み。                 |
| **詳細データ取得**     | **進行中 (25%)** | 934件が料金詳細・アメニティ・レビュー断片の取得を完了。                  |
| **アクセス情報**       | **進行中 (37%)** | 1,402件に最寄り駅距離が登録済み。                                        |
| **AI説明文生成**       | **稼働中 (NEW)** | Gemini 2.5 Flashにより自動リライト開始（現在バックグラウンドで進行中）。 |
| **AI口コミ生成**       | **実装待ち**     | 生成用スクリプトの準備完了。説明文生成の完了後に順次開始。               |
| **特集記事コンテンツ** | **未着手**       | `lh_feature_articles` は現在0件。                                        |

## 3. データベース構造（主要テーブル）

- `lh_hotels`: ホテルの基本情報、料金、AI説明文、スクレイピング済み断片（`review_snippets`）。
- `lh_reviews`: AI生成されたオリジナル口コミ（`is_verified`, `is_cast` フラグあり）。
- `lh_amenities` / `lh_hotel_amenities`: 設備・備品マスターとその紐付け。
  - **NEW**: キーワードベースの `fill_missing_amenities.cjs` により、取得済み紹介文からタグを自動補完済み。
- `lh_services` / `lh_hotel_services`: サービス（デリバリー可、外出可など）のマスタ。
- `lh_feature_articles`: エリア別・機能別特集記事。

## 4. 実行済み・利用可能なスクリプト (scripts/ 配下)

- `scrape_hotel_data.cjs`: Phase 1（名称、住所、電話番号の取得）。
- `scrape_phase2_details.cjs`: Phase 2（料金、アメニティ、レビュー断片の深掘り取得）。
- `generate_ai_descriptions.cjs`: **(NEW)** Gemini APIを使用して高品質な紹介文を自動生成。
- `fill_missing_amenities.cjs`: 紹介文から「Wi-Fi」「大型TV」等のタグを自動抽出。
- `tmp/check_hotel_progress.ts`: 現在の進捗（カウント）を確認する用。

## 5. 今後のタスクロードマップ（引き継ぎ用）

### TASK A: データ補完・クレンジング（最優先）

1.  **Phase 2 Scraping の完遂**:
    - 残り約2,800件の詳細スクレイピングを実行。
    - 実行コマンド: `node scripts/scrape_phase2_details.cjs`
2.  **AI説明文・キャッチコピーのバッチ生成**:
    - `node scripts/generate_ai_descriptions.cjs` を常時稼働させ、`ai_description` を全件埋める。
3.  **料金データの正規化 [完了済]**:
    - `scripts/normalize_prices.cjs` を実行完了。
    - `price_details` (JSON) から「休憩最安値」等を取り出し、ソート・表示用の数値フィールドへ展開。

### TASK B: AIコンテンツ錬成

1.  **AI口コミ生成の実装**:
    - `review_snippets` (スクレイピングした断片的な感想) を元に、Geminiで体験談として再構築（リライト）。
    - 複数のペルソナ（20代女子会、出張利用等）でバリエーションを持たせる。

### TASK C: UI/UX & メディア戦略

1.  **店舗詳細ページの完成**:
    - `src/app/(protected)/sweetstay/hotel/[id]` において、取得したアメニティ（アイコン付）やAI口コミを美しく表示。
2.  **エリア別特集の自動生成**:
    - 取得した「設備タグ（露天風呂、TVサイズ等）」とエリアを組み合わせ、特集記事を自動生成。

## 6. 開発再開時の注意点

- **API制限**: Gemini 2.5 Flashを利用中（無料枠）。レート制限（1分15回程度）を考慮し、スクリプトには10-20秒のディレイを入れている。
- **環境変数**: `GEMINI_API_KEY` を `.env` および `.env.local` に設定済み。
- **データ不整合**: スクレイピング失敗時は `raw_description` が空になるため、その場合は再試行または手動補完が必要。
